const dummy = (blogs) => {
  return 1;
};

const totalLikes = (blogs) => {
  // Should return the sum of likes of all blog posts in the blogs array
  return 0;
};

module.exports = {
  dummy,
  totalLikes,
};
